package model.faction;

public class SpellCard extends Card {
    public SpellCard(String name, boolean isUnitCard, CardAbility ability, int power) {
        super(name, isUnitCard, ability, power);
    }
}
